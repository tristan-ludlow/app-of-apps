apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "argo-apps.fullname" . }}-test-connection"
  labels:
    {{- include "argo-apps.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "argo-apps.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
